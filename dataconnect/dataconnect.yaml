specVersion: "v1"
serviceId: "kanban-board"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kanban-board-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
